{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-scope.sh\" review-config.yaml target_paths \"review-results.tsv review-scan.json\" \"This file is outside the review target scope. Modifying non-target files during a review loop may introduce untracked changes. Confirm before proceeding.\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
