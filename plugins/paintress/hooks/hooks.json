{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if continent-config.yaml exists in the current directory. If it does, read the 'repository' field. If the Edit/Write target file is NOT within that repository path and is NOT 'journal.tsv' or 'continent-config.yaml', warn: 'WARNING: This file is outside the expedition target repository. Expedition scope is limited to the configured repository. Proceed only if intentional.'"
          }
        ]
      }
    ]
  }
}
