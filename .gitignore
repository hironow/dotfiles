# Homebrew
Brewfile.lock.json

# dotfiles-specific private
refs/
.env.keys

# node lockfiles (not tracked in dotfiles)
package.json
package-lock.json
bun.lock

# Nix
result

# autodesign
design-results.tsv
design-config.yaml
run.log

# Azure
.azurite/

# IaC
.terraform
.terraform.lock.hcl
*.tfstate
terraform.tfstate
terraform.tfstate.backup
*.tfplan

# Cargo
/target/

# AI coding
.serena/cache/
.sync-manifest.json

# Model
*.pt

# claude local settings (broader than global)
.claude/*.local.*

# paintress expedition state
journal.tsv
gradient.json
lumina.md
gommage.md
continent-config.yaml
